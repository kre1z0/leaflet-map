import React from "react";
import PropTypes from "prop-types";

import styles from "./Legend.scss";

export const Legend = () => {
  return <div className={styles.legend}>ggwp no re</div>;
};

Legend.propTypes = {
  component: PropTypes.bool,
};
